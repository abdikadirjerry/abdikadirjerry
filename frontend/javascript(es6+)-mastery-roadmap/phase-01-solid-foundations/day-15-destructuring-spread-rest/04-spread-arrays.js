// Challenge 1 — Clone array
const arr = [1, 2, 3];

const clone = [...arr];

console.log(clone);

// Challenge 2 — Merge arrays
const a = [1, 2];
const b = [3, 4];

const merged = [...a, ...b];

console.log(merged);

// Challenge 3 — Add element using spread
const numbers = [10, 20, 30];

const newNumbers = [...numbers, 40];

console.log(newNumbers);
