// Block Scope Example

if (true) {
  let age = 25;
  const city = "Hargeisa";

  console.log("Inside block:", age, city);
}

// console.log(age);
// console.log(city);
// These would cause errors because they are block scoped
