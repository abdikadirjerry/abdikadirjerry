// 🟢 Challenge — Memory Thinking

let sum = 0;

const newArray = [];

/*
📌 Explanation:

1️⃣ sum variable:
- Uses only ONE memory space
- Space Complexity = O(1) (constant space)

2️⃣ newArray:
- Can grow depending on data
- If we keep adding items → memory increases
- Space Complexity = O(n)

👉 Conclusion:
newArray uses MORE space than sum
*/
