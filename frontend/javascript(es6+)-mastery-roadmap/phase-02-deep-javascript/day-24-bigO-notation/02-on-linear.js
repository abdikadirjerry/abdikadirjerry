// 🟢 Challenge — O(n)

const arr = [1, 2, 3, 4, 5];

for (let i = 0; i < arr.length; i++) {
  console.log(arr[i]);
}

/*
📌 Time Complexity: O(n)

📌 Explanation:
- Loop runs once for each element.
- If array has n elements → loop runs n times.

👉 More data = more steps (linear growth)
*/
