# HTTP Lifecycle

1. User enters URL
2. DNS resolves domain to IP
3. Browser sends HTTP request
4. Server processes request
5. Server sends response
6. Browser renders UI

## Notes

- Request = asking for data
- Response = server reply
- Happens every time you load a page
